{
  "name": "task-tracker-cli",
  "version": "1.0.0",
  "bin": {
    "task-cli": "bin/cli.js"
  },
  "description": "Simple task tracker cli program",
  "main": "lib/index.js",
  "scripts": {
    "build": "npm run clean && tsc --build",
    "clean": "tsc --build --clean",
    "start": "npm run build -- --watch"
  },
  "keywords": [
    "task-traker",
    "cli",
    "task-management-cli"
  ],
  "author": "Nikesh",
  "license": "ISC",
  "devDependencies": {
    "@types/debug": "^4.1.12",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "debug": "^4.4.1"
  }
}
